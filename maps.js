const choosedestbutton = document.getElementsByClassName("tf-button");
for (let i = 0; i < tf-button.length; i++) {
    tf-button[i].addEventListener("click", displaydestinfo);
}

function displaydestinfo(dest) {
    const
}